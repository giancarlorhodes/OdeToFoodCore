@page
@model OdeToFoodCore.Pages.Restaurants.ListModel
@{
    ViewData["Title"] = "List of Restaurants";
}

<h1>Restaurants</h1>


<form  method="get">

    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="input-group-btn">
               <button class="btn btn-default">
                   <i class="glyphicon glyphicon-search"></i>
               </button>
            </span>
        </div>
    </div>

</form>

@*<table class="table">

    @foreach (var r in Model.Restaurants)
    {       
        <tr>
            <td>@r.Name</td>
            <td>@r.Location</td>
            <td>@r.Cuisine</td>
            <td>
                <a class="btn btn-lg" asp-page="./Detail" asp-route-restaurantId="@r.Id">
                    <i class="glyphicon glyphicon-zoom-in"></i>
                </a>
                <a class="btn btn-lg" asp-page="./Edit" asp-route-restaurantId="@r.Id">
                    <i class="glyphicon glyphicon-edit"></i>
                </a>
                <a class="btn btn-lg" asp-page="./Delete" asp-route-restaurantId="@r.Id">
                    <i class="glyphicon glyphicon-trash"></i>
                </a>
            </td>
        </tr>   
    }

</table>*@


@foreach (var _restaurant in Model.Restaurants)
{
    <partial name="_Summary" model="_restaurant" />
}

<a asp-page=".\Edit" class="btn btn-primary">Add New</a>

@section footer{ 
<div>@Model.Message</div>
}